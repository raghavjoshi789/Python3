1. I have a shell script file.sh that runs a file1.py Any idea how do I create log files and trace files?

2. Question 1: Write a program that calculates and prints the value according to the given formula:
Q = Square root of [(2 * C * D)/H]
Following are the fixed values of C and H:
C is 50. H is 30.
D is the variable whose values should be input to your program in a comma-separated sequence.

Example:
Let us assume the following comma separated input sequence is given to the program:
100,150,180

The output of the program should be:
18,22,24

3. How can I detect file change in python?
PS. there will be many file(20 - 30) and they can exceed 1000lines and performance is a concern?

4.  "Machine Learning based detection of DDoS attack" ?
